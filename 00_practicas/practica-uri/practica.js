//declarada funcion.


function numeroImpares (numero){ //primera letra minuscula, segunda palabra mayuscula
    
    console.log(numero); 
    
    /*
    for ( var i = 1 ; i >= numero.length ; i++){  
        if (i % 2 != 0){
            numero ++;
        
        }
    }
    return numero;
    */ //bloque de codigo grande. 
}
//control s, para guardar pero el mouse en la pantalla.
//ejecutar una funcion, los parentesis siempre son necesarios una funcion para ejecutar una funcion
numeroImpares (23)